# Wireframes (historical)

Pre-Figma visual history for customer, staff, and admin screens.

**Agents must not implement from these HTML files** when a Figma frame exists
for the screen. Prefer `.claude/contracts/FIGMA.md` and Untitled UI under
`src/components/base/` and `src/components/application/` (see UNTITLED.md).

These files remain for reference and migration mapping only.
