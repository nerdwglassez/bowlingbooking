import { Righteous } from 'next/font/google'

/** Righteous – used for the booking flow headline "Reserve Your Lane" to match Figma. */
export const righteous = Righteous({ weight: '400', subsets: ['latin'] })
