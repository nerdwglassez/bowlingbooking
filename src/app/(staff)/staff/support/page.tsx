// /staff/support — placeholder until the support surface is built.

import { StaffSupportPanel } from '@/components/patterns/staff-support-panel'

export default function StaffSupportPage() {
  return <StaffSupportPanel />
}
