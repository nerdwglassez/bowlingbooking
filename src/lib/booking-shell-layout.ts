/** Shared horizontal inset for booking shell chrome (header, content, footer bleeds). */
export const BOOKING_SHELL_PX = 'px-4 md:px-6 lg:px-8'

/** Negative margin to edge-bleed chrome/footer against BOOKING_SHELL_PX padding. */
export const BOOKING_SHELL_NEG_MX = '-mx-4 md:-mx-6 lg:-mx-8'
